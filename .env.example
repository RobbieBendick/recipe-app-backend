APP_ENV=development
PORT=8080
ALLOWED_ORIGIN=http://localhost:5173

# Neon Postgres connection string (Dashboard → Connect → copy URI)
# Use sslmode=require
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# Optional
# DATABASE_POOL_MAX_CONNS=10
# HTTP_WRITE_TIMEOUT_SEC=120
